##### 		--发帖页面：（按钮在主页，或者是课程组中）（若相关了课程，该课程必须是自己有成绩的课程）	1

​			若该贴子相关了某个课程，则必须进行负载评价（打分），（相当于该贴子是一个评价贴，不是任意贴）

​			每个学生对每个课程只能相关一次

​			相关课程（可以没有，默认有一个），标题，内容，图片（可以没有），发布者，发布时间	1





计算评分（课程列表页面）	1

进入课程列表页面时去计算一次课程平均负载评分即可	1

```

```



BUG：选择课程时都显示体育	1



优化：	每个学生 只能评价 一次 每种课程	1

在发布贴子的时候根据相关课程code和发布人no确保一个学生只评价一种课程	1







##### 		--专属小组

​			在课程组页面有一个“进入专属小组讨论按钮”。（进入课程组页面后会根据登录者学号/工号判断是否在成绩表中有相关课程（代码）的记录，如果有则展示按钮“加入专属课程小组”）	1

​			若是教师工号则判断教师是否教授该课程	1

​			

​			（留言板）

留言者账号，留言者姓名，留言内容，留言时间，留言类型（教师/学生），留言课程	1



```
const response = await axios.get(`/message/message/${courseCode.value}`)
```

```
await axios.post('/message/message', {
  courseCode: courseCode.value,
  content: newMessage.value.trim(),
  userAccount: currentUser.value
})
```



注意留言类型（教师和学生不同）

​			学生教师都可以直接留言（教师留言置顶，不可修改，可自己删除）	1



删除留言		1





优化：  贴子内容的展示格式			1

​		white-space: pre-wrap;

​		

BUG：关联课程的贴子发布时必须评分		1

​	不关联课程的贴子发布时不需要评分

​		前端不去警告（），	1

​		后端逻辑控制		1

s

优化:在课程组中搜索时，仅搜索相关课程的贴子	1



（教师用户用户名后显示特别标签表示教师）	1



教师的评论应该有特殊样式	1

​	应该在数据库的评论表中维护一个评论者类型（学生/教师），查询该字段值进行样式区别化

​	现在，判断reviewer_no字段值的前四位是否是“6666”，来进行样式区别化



教师不能发帖，				1

​	前端警告 + 后端认证	



教师可以在专属小组留言，（置顶）		1
	前端教师留言区别化，后端教师留言置顶

​	







