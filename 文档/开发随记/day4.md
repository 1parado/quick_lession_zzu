明日任务：

​	将更新联系方式弹窗位置调整1

​	侧边导航栏向各页面导航1

​	主区域不着急做1

​	做学生管理1、教师管理、等等



今天做了什么

​	将更新联系方式弹窗位置调整

​	修复一个bug：手机号格式不对不能更新

​	侧边栏导航到各个页面

​		前端借助全局路由守卫禁止访问无权限的页面（先不做，因为要对每个页面进行角色规定）

​		制作组件禁用by权限

​		将主页默认区域也定义为一个组件，默认展示

​		头部区域提供回到主页按钮

​	做学生管理（对学生表的CRUD）

​		分页查询，前端传来页码和单页数据量，后端返回数据和总量,

​			搞了半天pageHealper，竟然是版本用错了，我用的1.4.5就不行，换成1.4.7就可以了

​		写详情，通过 `#default="scope"` 获取当前行的完整数据，detailStudent(scope.row)

​		写修改，传数据时一定注意，否则会报一些意想不到的问题，改学号时账号密码要改吗？要

​			修改学号时，在users表中找对应账号的数据，将账号和密码修改成新学号（重置密码）

​		加一个学生/教师 修改密码的功能

​			注意，管理员修改学号后，账号密码重置为新学号

​			发送put请求（携带一个password参数）一直报错，得添加`'Content-Type': 'text/plain'` 请求头，当是一个JS对象时axios会自动设置请求头Content-Type: application/json

​		写删除（批量删除先不做）

​			删除学生时，要顺便删除users表中的对应数据

​		写新增，

​			写入学生表前，先把根据sno写入users表，然后拿到users表的id写给学生表的user_id.



​		需要健壮新增和修改，做验证（前后端都需验证）

​			前端负责做格式验证，确保输入必要数据的正确格式

​			利用 :rule定义规则，:ref创建引用，prop属性，validate()方法

​			后端负责做数据验证，确保唯一数据唯一，有效数据有效



今日总结：

​	今天没有做教师管理，但是一法通万法通，相信明天一定会做的更加得心应手

​	今天主要制作学生表的CRUD，实现很简单，但是小错误不断，导致做的很慢，

​	分页查询时pageHeapler用错了版本

​	发送put请求时得添加`'Content-Type': 'text/plain'` 请求头，当是一个JS对象时axios会自动设置请求头Content-Type: application/json

​	注意改数据是级联修改登录用户表

​	验证利用了rule定义规则，:ref创建引用，prop属性，validate()方法

​	总之主要时间都用在前端上了，本来就不熟悉，只是使用AI + ElementPlus 进行页面制作

​	

明日计划：

​	做完管理员的教师管理、课程管理、公告管理，借鉴学生管理应该勉强能做完

​	抢课任务管理先不做

​	大致做完管理员端的特殊功能后，开始做学生端（本系统核心在学生端抢课上）

​	再之后的教师端更加侧重与一个扩展，成绩导入导出，课程表导入导出等等