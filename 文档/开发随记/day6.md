今日规划：

​	完成增加、修改时的时间选择器选择时间

​	将“已发布”页面 CV 到 其他两个页面

​	选课任务管理

​	

今天做了什么

删除页面 需要 恢复操作、彻底删除操作

发布时间这一块有BUG

​	这里看要不要做定时发布的功能（可以做，先不做）

​	若是不做

​		对于新增公告，发送时强制改变发布时间为当前时间1

​					保存时只要在当前时间后即可，此时的发布时间仅仅充当一个标志的作用1

​		对于最新公告显示，只显示最新的发布的数据1

​		对于已发布页面

​			已发布的时间无法被修改1

​			已发布页面加载数据时按时间排序倒叙1

​		对于草稿箱页面

​			展示的应该叫做编辑人、预计发布时间（暂时没做定时）1

​			详情页也得改1

​			额外添加一个发布按钮，将状态改变为已发布，并且将发布时间改为当前时间1

​		对于已删除页面

​			不需要发布时间，要删除时间，也就是说，其他页面删除时，改变时间为当前时间1

​			恢复时恢复到草稿状态时间变为当前时间1



总结：公告管理模块小东西还是很多的，分状态的CRUD，以及状态之间的转换，在写的时候我是编写便规划，导致后端的状态转换比较混乱。应该要提前做好规划，提前将表分状态后的CRUD，状态转换都写好，但是我习惯与开发从前端到后端，这样有一条清晰的线，但是一根一根线最终错综复杂，也需要适当的规划



选课任务管理
选课任务列表管理，添加选课任务（调整选课参数：选课时间、选课开关（定时开关）），修改，删除，查询所有选课任务

​	页面展示1

​		秒杀课程（连表），名额（这个名额一定不会大于课程余额，当课程的容量和余额为空时说明这是必修的、不需要选择的、不会参加选课的，但是可以查看，这里的名额就是说，我们一次性可以有选择的放出一部分名额而不是全部放出，这个名额的值在选课任务管理中设置，需要连表查询设置最大名额 ），开始时间、结束时间、秒杀状态

​	详情页面 1

​		秒杀任务 id 、名额、开始时间、结束时间、状态、相关课程表课程名、代码、专业（重要）容量、余额，开设学期、连表（教师表连接课程表）查相关教师tno、name

​	新增页面

​		下拉选择一个isSeckill字段值为1的课程，显示课程的容量和余额，新增时自定义一个名额，开始时间、结束时间，状态不可自定义根据时间变化

​		新增时以课程代码为唯一的一个标准，先判断有误该课程代码的任务

​		注意

​			 id 不用填 status不用填

​			名额一定要小于等于余额

​			课程相关的来一个下拉列表选择课程代码（从课程表中去重查找课程代码字段）1

​				只选择isSeckill值为1的课程1

​				去根据选择的课程代码查询相关的信息赋值到data上1

​			这个教师信息是绑定在课程上的，所以不需要下拉列表

​			

​			根据course_id去判断任务是否唯一，是否可以新增

​			新增时根据courseCode拿到course_id



​	修改页面

​		课程不可变，可以修改名额，开始时间和结束时间

​		修改名额时，不能超过余额

​		开始时间禁用之前



​	删除页面

​		删除秒杀任务即可

​		注意：关联了好多东西，

​			当相关的课程要进行删除时，级联的删除秒杀任务；

​			当相关的教师要进行删除时，要级联的删除相关的课程再级联的删除相关的秒杀任务



​	优化，新增课程时，可以选择教师工号

​	

​	BUG: 不合法的数据显示在页面中

​		前端数据
​			

```
	id: '',
    startTime: '',
    endTime: '',
    status: '',
    store: '',
    
    capacity: '',
    remain: '',
    name: '',
    courseCode: '',
    college: '',
    semester: '',
    
    teacherName: '',
    tno: ''
```



```
/**
 * 秒杀任务ID
 */
private Integer id;

/**
 * 开始时间
 */
private LocalDateTime startTime;

/**
 * 结束时间
 */
private LocalDateTime endTime;

/**
 * 课程状态
 */
private String status;

/**
 * 名额（放出的名额）
 */
private Integer store;

/**
 * 容量
 */
private Integer capacity;

/**
 * 剩余名额
 */
private Integer remain;

/**
 * 课程名称
 */
private String name;


/**
 * 课程代码
 */
private String courseCode;

/**
 * 开课学院
 */
private String college;

/**
 * 开课学期
 */
private String semester;

/**
 * 任课教师名称
 */
private String teacherName;

/**
 * 任课教师工号
 */
private Long tno;
```
