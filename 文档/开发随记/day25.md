注意：之后优化：多线程处理异步数据库的保存	



RocketMQ默认会有多个消费线程：

默认最小线程数：20

默认最大线程数：64

默认可以并行处理多个消息



预选课（购物车）	

​		在限时选课页面的上方显示一栏 “选课信息预填，（小字）提前填写选课信息，课程开放时快速提交（比手动延迟30s？公平一些）”，去填写按钮

​		填写选课信息窗口，显示所有未开始的限时课程，可以批量选择，

​		保存到（个人信息 + 选择信息）session？



​	不做自动了改做批量提交

​		预选课程保存到数据库中	1

​		加载当前页面时同步保存到Redis中，这样既保证了用户退出登录后预选信息的持久性，又保证可以快速获取这些信息。	1

​		修改/新增选课任务后要更新缓存	1

​		前端提供预选抢课按钮，就是去后端拿到当前学生预选的所有课程，然后按顺序去抢课	1



预选课程未开始抢课时的合理提醒



提醒这一块没做好：优化
	在获取动态地址的时候，遍历每一个课程，判断是否有未开始的，若都是则返回“选课未开始”	1

​	在前端轮询结果时，显示选课成功的课程，选课失败失败的课程	1



清除器修理	1



测试预约一个成功一个失败	1

​	失败的有提醒：绿色带课程号码



关于提醒应该一并提醒	1



测试预约都失败	1

​	没有判断时间就都成功、

​	显示抢课失败





智能选课推荐（在成功选择一门课后）

​		基于内容：智能推荐相同老师、相似领域的课程

​		基于用户： “选了这门课的同学，也选了...”







用户体验

智能化（AI驱动）

数据化

生态化

结合一下新扩展功能，总结如下：

接入一个选课论坛：(搞一个小程序？？？)

​	自动使用当前账号登录（学生/教师，没有管理员）

​	主页：

​		查看学生发布的帖子（标题，发布者，内容，发布时间，评价的课程（当然，也可以没有，纯随便发的也可以））

​	发帖按钮：

​		发帖页面（可以带个图片？选择评价的课程（特定学生）？）

​	课程评价：

​		展示所有的课程，每个课程可以点进去，是一个圈子（类似贴吧）

​		在课程圈子内，只有选择过该课程并且通过考试（只要有成绩）的学生才能进行**评价**

​		其他学生可以查看评价。

​		在课程圈子内使用智能化课程负载预测，利用NLP分析课程评论中的关键词（如“作业量巨大”、“每周都要读paper”、“考试难度高”），得出一个综合负载评分

​	圈子：（该页面与课程页面类似，不过该页面只显示自己加入的（选择过该课程的），且具体用法、目标不同）

​		选择某个课程后可以加入给课程的专属论坛（具体和课程页面差不多），在该论坛中与评价无关，所有加入该课程的学生可以自由发帖在该专属论坛中进行讨论，互相交流。

​	我的：

​		（暂时不写）



对于教师用户的特殊之处（用户名后显示特别标签表示教师）

​	教师不能发帖，教师不能评价课程（可以看），教师的圈子默认只显示自己所教授的（默认加入，在该专属论坛内可以发消息）

